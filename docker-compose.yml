---
version: "0.7"
services:
  porkchop:
    image: mrkrinlee/porkchop:latest
    build:
      context: .
    container_name: porkchop
    volumes:
      -  /mnt/user/appdata/porkchop:/config
    ports:
      - 22319:22319
